#include <iostream>
#include "einsbishundert.h"

int main()
{
    int number;
    std::cout<<"Geben Sie die Nummer an bis wann das Array erstellt werden soll:";
    std::cin>>number;
    einsbisN(number);

return 0;
}

